# AWS Bedrock

LangChain.js supports integration with AWS Bedrock. Check [Amazon Bedrock](https://aws.amazon.com/bedrock/).

Here's an example:

```bash npm2yarn
npm install aws-sigv4-fetch
```

import CodeBlock from "@theme/CodeBlock";
import BedrockExample from "@examples/models/llm/bedrock.ts";

<CodeBlock language="typescript">{BedrockExample}</CodeBlock>